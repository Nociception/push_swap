#include "push_swap.h"

/*
void    algo_length(t_stack **A, t_stack **B, int len)
{
    int ssot;

    ssot = stack_sorted_ontop(*A);
    if (len == 2 && (*A)->index)
        swap_a(A);
    else if (len == 3 && !ssot)
        algo3(A);
    else if (len == 4 && !ssot)
    	algo_four(A);
	else if (len == 5 && !ssot)
		algo5(A, B);
}
*/